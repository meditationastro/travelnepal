export default function OrdersManager() {
  return (
    <div className="bg-white rounded-2xl border border-stone-100 p-6">
      <h3 className="font-semibold text-stone-900">Orders</h3>
      <p className="text-sm text-stone-500 mt-1">
        Orders module placeholder (build-safe). Hook this up to your payments provider (Stripe/Khalti/PayPal) later.
      </p>
      <div className="mt-4 text-xs text-stone-400">Tip: keep admin routes compiling even if features aren’t finished.</div>
    </div>
  );
}
