[build]
  command = "npx prisma generate && npx prisma db push && node prisma/seed.js && next build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
  NEXT_TELEMETRY_DISABLED = "1"
  

[[plugins]]
  package = "@netlify/plugin-nextjs"
